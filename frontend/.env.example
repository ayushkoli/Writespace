# Leave empty when using Vercel rewrites (recommended). API calls go to /api/v1 on the same domain.
# Set this only if the frontend talks directly to a separate backend URL.
# VITE_API_URL=https://your-backend.onrender.com/api/v1
